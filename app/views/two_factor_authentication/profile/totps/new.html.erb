<div class="max-w-2xl mx-auto">
  <div class="card bg-base-100 shadow-xl">
    <div class="card-body">
      <% if Current.user.otp_required_for_sign_in? %>
        <div role="alert" class="alert alert-warning mb-6">
          <svg xmlns="http://www.w3.org/2000/svg" class="stroke-current shrink-0 h-6 w-6" fill="none" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-3L13.732 4c-.77-1.333-2.694-1.333-3.464 0L3.34 16c-.77 1.333.192 3 1.732 3z" />
          </svg>
          <div>
            <h3 class="font-bold">Want to replace your existing 2FA setup?</h3>
            <div class="text-xs">Your account is already protected with two-factor authentication. You can replace that setup if you want to switch to a new phone or authenticator app.</div>
            <div class="text-xs font-semibold mt-2">Do you want to continue? Your existing 2FA setup will no longer work.</div>
          </div>
        </div>

        <div class="mb-6">
          <%= button_to "Yes, replace my 2FA setup", two_factor_authentication_profile_totp_path, method: :patch, class: "btn btn-warning" %>
        </div>

        <div class="divider"></div>
      <% end %>

      <h1 class="card-title text-3xl mb-6">Upgrade your security with 2FA</h1>

      <div class="space-y-6">
        <div>
          <h2 class="text-2xl font-semibold mb-2">Step 1: Get an Authenticator App</h2>
          <p class="mb-2">First, you'll need a 2FA authenticator app on your phone. <strong>If you already have one, skip to step 2.</strong></p>
          <p><strong>If you don't have one, or you aren't sure, we recommend Microsoft Authenticator</strong>. You can download it free on the Apple App Store for iPhone, or Google Play Store for Android. Please grab your phone, search the store, and install it now.</p>
        </div>

        <div>
          <h2 class="text-2xl font-semibold mb-2">Step 2: Scan + Enter the Code</h2>
          <p class="mb-4">Next, open the authenticator app, tap "Scan QR code" or "+", and, when it asks, point your phone's camera at this QR code picture below.</p>

          <figure class="flex flex-col items-center">
            <div class="card bg-base-200 p-4">
              <%= image_tag @qr_code.as_png(resize_exactly_to: 200).to_data_url, alt: "QR Code", class: "mx-auto" %>
            </div>
            <figcaption class="text-sm text-base-content/70 mt-2">Point your camera here</figcaption>
          </figure>
        </div>

        <%= form_with(url: two_factor_authentication_profile_totp_path, class: "space-y-6") do |form| %>
          <div class="form-control w-full">
            <label class="label">
              <span class="label-text">After scanning with your camera, the app will generate a six-digit code. Enter it here:</span>
            </label>
            <%= form.text_field :code, required: true, autofocus: true, autocomplete: :off, class: "input input-bordered w-full text-center text-2xl tracking-widest font-mono", maxlength: 6, pattern: "[0-9]{6}", placeholder: "000000" %>
          </div>

          <div class="form-control mt-8">
            <%= form.submit "Verify and activate", class: "btn btn-primary btn-lg w-full" %>
          </div>
        <% end %>
      </div>

      <div class="card-actions justify-end mt-6">
        <%= link_to "Back", root_path, class: "btn btn-ghost" %>
      </div>
    </div>
  </div>
</div>
